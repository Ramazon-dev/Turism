import 'package:flutter/material.dart';

class PasswordSms extends StatelessWidget {
  const PasswordSms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PasswordSms"),
      ),
    );
  }
}
